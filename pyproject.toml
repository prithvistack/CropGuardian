[project]
name = "cropguardian-ai"
version = "0.1.0"
description = "Production-oriented tomato disease detection ML repository"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
